apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-kube-chart.fullname" . }}
  labels:
    {{- include "my-kube-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-kube-chart.selectorLabels" . | nindent 4 }}
