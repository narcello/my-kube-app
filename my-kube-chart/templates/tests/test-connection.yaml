apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "my-kube-chart.fullname" . }}-test-connection"
  labels:
    {{- include "my-kube-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "my-kube-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
